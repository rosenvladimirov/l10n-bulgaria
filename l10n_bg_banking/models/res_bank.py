from odoo import fields, models


class ResBank(models.Model):
    _inherit = 'res.bank'

    # Basic bank information fields can be added here if needed
    # For now, we inherit the base model without additional fields
    pass
