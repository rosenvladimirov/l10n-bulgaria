<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron за автоматични Z отчети -->
        <record id="ir_cron_generate_z_reports" model="ir.cron">
            <field name="name">Generate automatic Z reports</field>
            <field name="model_id" ref="l10n_bg_erp_net_base.model_fiscal_printer_device"/>
            <field name="state">code</field>
            <field name="code">model._cron_generate_z_reports()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Cron за изчистване на статусна история (седмично) — преместен
             в l10n_bg_erp_net_base (транспортен concern), 19.0.16.0.0. -->

        <!--
            Auto-close stuck fiscal sessions — Наредба Н-18 ограничава
            фискалната сесия на ≤24h. Cron-ът тече всеки ден в 23:55
            и затваря всички fiscal.session с opened_at < now-23h
            (1h headroom). Pull sales + Z + close при възможност, или
            mark closed_partial + alert до отговорния потребител.
        -->
        <record id="ir_cron_auto_close_stuck_fiscal_sessions" model="ir.cron">
            <field name="name">Auto-close stuck fiscal sessions (Н-18 ≤24h)</field>
            <field name="model_id" ref="model_fiscal_session"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_close_stuck_sessions()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 23:55:00')"/>
        </record>

    </data>
</odoo>
