<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_leave_balance_year_list" model="ir.ui.view">
        <field name="name">hr.leave.balance.year.list</field>
        <field name="model">hr.leave.balance.year</field>
        <field name="arch" type="xml">
            <list string="Leave Balance by Year" create="0" edit="0" delete="0"
                  default_order="employee_id, leave_type_id, year desc">
                <field name="employee_id"/>
                <field name="company_id" groups="base.group_multi_company" optional="hide"/>
                <field name="leave_type_id"/>
                <field name="year"/>
                <field name="allocated_days" sum="Total"/>
                <field name="taken_days" sum="Total"/>
                <field name="remaining_days" sum="Total"
                       decoration-danger="remaining_days &lt; 0"
                       decoration-warning="remaining_days == 0"/>
            </list>
        </field>
    </record>

    <record id="view_hr_leave_balance_year_search" model="ir.ui.view">
        <field name="name">hr.leave.balance.year.search</field>
        <field name="model">hr.leave.balance.year</field>
        <field name="arch" type="xml">
            <search string="Search Leave Balance by Year">
                <field name="employee_id"/>
                <field name="leave_type_id"/>
                <field name="year"/>
                <filter string="Remaining" name="has_remaining"
                        domain="[('remaining_days', '&gt;', 0)]"/>
                <group>
                    <filter string="Year" name="group_year"
                            context="{'group_by': 'year'}"/>
                    <filter string="Employee" name="group_employee"
                            context="{'group_by': 'employee_id'}"/>
                    <filter string="Leave Type" name="group_leave_type"
                            context="{'group_by': 'leave_type_id'}"/>
                    <filter string="Company" name="group_company"
                            context="{'group_by': 'company_id'}"
                            groups="base.group_multi_company"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_hr_leave_balance_year" model="ir.actions.act_window">
        <field name="name">Leave Balance by Year</field>
        <field name="res_model">hr.leave.balance.year</field>
        <field name="view_mode">list</field>
        <field name="search_view_id" ref="view_hr_leave_balance_year_search"/>
        <field name="context">{'search_default_group_year': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No leave allocations or leaves found
            </p>
            <p>
                This list breaks leave balances down by calendar year, so you can
                track remaining days carried over from previous years.
            </p>
        </field>
    </record>

    <menuitem id="menu_hr_leave_balance_year"
              name="Leave Balance by Year"
              parent="hr_holidays.menu_hr_holidays_management"
              action="action_hr_leave_balance_year"
              sequence="6"/>

</odoo>
