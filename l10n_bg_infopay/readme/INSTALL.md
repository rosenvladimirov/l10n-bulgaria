This module requires the
[requests](https://pypi.org/project/requests/) Python library (usually
already installed with Odoo).

The ``l10n_bg_bank_wallet`` module must be installed first – it provides
secure encrypted storage for the InfoPay API token.
